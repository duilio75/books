# Python
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/

# Virtualenvs
.env/
venv/
.venv/

# Node (frontend is built in its own stage; never copy local deps/build)
node_modules/
frontend/node_modules/
frontend/build/

# Secrets & local config (passed in at runtime instead)
.env

# Local databases
*.sqlite3

# User uploads live in a persistent volume, not the image
media/

# Media backup archives (created by `make backup-media`)
backup/

# VCS & editor
.git/
.gitignore
.idea/
.vscode/

# Docker
Dockerfile
.dockerignore
docker-compose.yml
